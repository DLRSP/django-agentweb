"""SDF domain — Structured Data Format (single-promoter, emerging).

Implemented behind a feature flag and **off by default**: included for
dogfooding/demo and external-adoption visibility, not for production
use until the format matures.
"""
